# kuma · 使用说明

## 一、这是什么

kuma 是《舰队 Collection》的信息工作台

kuma 只读取游戏自己产生的通信

系统要求：Windows 10 / 11 的 64 位版本。便携版，不需要安装

## 二、安装

1. 下载压缩包后，先右键它 →「属性」→ 如果下方有「解除锁定」就勾上 → 确定
2. 把整个文件夹解压出来。不要只把 `kuma.exe` 拖出来，它需要同一文件夹里的其他文件
3. 双击 `kuma.exe` 启动。开头有一段入场动画，点一下跳过，「设置」里可关。
   想要桌面图标的话，右键它「发送到 → 桌面快捷方式」
4. 卸载就是删掉这个文件夹。（你的记录另存在系统的应用数据目录里，见第四节）

### 首次运行会有安全提示

kuma 没有购买代码签名证书，Windows 首次运行时可能弹出蓝色的「Windows 已保护你的电脑」窗口。
点窗口里的「更多信息」，再点「仍要运行」即可

从浏览器下载的压缩包解压出来的程序还会带一个「来自网络」的标记；如果每次启动都被拦，
可以在**解压前**右键压缩包 →「属性」→ 勾选「解除锁定」→ 确定，再重新解压

kuma 的源码是公开的，你也可以自己构建。如果不放心，可以先用在线杀毒扫描服务检查一下安装包

## 三、连上游戏（代理）

游戏只接受来自日本的网络访问，所以要先让 kuma 走你已有的代理工具。
打开顶栏的「设置」→「代理」，按你在用的工具选一档：

| 你在用的 | 在 kuma 里选 | 填什么 |
|---|---|---|
| 岛风GO | HTTP | 主机 `127.0.0.1`，端口 `8099`（这是默认值；若改过，以岛风GO 托盘图标右键「打开全局设置」里显示的「本地代理端口」为准） |
| SSR / Clash 这类本地代理 | SOCKS5 | 主机 `127.0.0.1`，端口按该工具显示的填（常见 `1080`） |
| 整机已经全局代理 | 直连 | —— |
| 手上有 PAC 脚本地址 | PAC 脚本 | 填那条 `http://…` 地址 |

填完即时生效。代理卡下方那行会显示这次应用的结果；
连不上时会写出具体原因

游戏页面一直白屏或卡在黑船画面时，用「设置」→「缓存修复」清一次缓存再试

## 四、你的数据在哪

kuma 的记录存在 `%APPDATA%\kuma`（把这一串粘进资源管理器地址栏就能打开）。
删掉程序文件夹不会动到它

用过改名前那一版（`%APPDATA%\kanso`）的话不用管：首次启动时旧目录会整个搬到新目录，
账本、设置与三份档案原样跟过来。万一搬不动（目录正被别的程序占着之类），
这一次仍旧读写旧目录，数据不会丢，下次启动再试一遍

「设置」→「完整备份与恢复」可以把历史记录、布局、通知规则、收藏与个人备注
打成一个文件；从备份恢复会覆盖当前内容并自动重启

备份文件里含配置，配置里可能有代理的账号密码。请像保管账号资料一样保管它

## 五、常见问题

- **游戏白屏、贴图错乱、卡在黑船画面** → 「设置」→「缓存修复」→「清理缓存并重启」。
  登录状态、历史记录、配置、语音档案与立绘档案都会保留
- **图鉴的季节限定台词里，播放钮为什么大多是灰的** → 那些是还没收集到的。
  台词清单是完整的（哪一年的哪个节日她说过什么，全列出来），
  而能播的只有**你自己在游戏里听到过**的那几句：游戏播一次，kuma 就从浏览器缓存里
  转存一份进「语音档案」，那一格就点亮。季节语音过季后游戏不再播放，
  所以这份档案是它唯一的去处。
  档案占用与清空入口在「设置」→「语音档案」
- **图鉴的立绘页为什么有些图是空的，末尾那几张「档案 … 留存」又是什么** →
  这一页摆的是**官方现在放着的**那几张。在游戏里打开她的图鉴页/编成页看一眼，
  图会被游戏自己下载，kuma 随即从浏览器缓存里转存一份进「立绘档案」。
  只搬已经在缓存里的；还没被游戏取过的那几张就是空的。
  末尾标「档案 ◯◯ 留存」的，是档案里留着、而官方现在已经不放的旧版本（换季或改绘之后）。
  点开看的是本机文件，卡上只写图种与留存月份。
  档案占用与清空入口在「设置」→「立绘档案」
- **档案里的语音、立绘、曲子可以拷给朋友吗** → 不行。那些都是游戏自己的资产，
  kuma 只是替你在本机留了一份，仅供你自己使用。把档案或缓存文件传播、散布出去，
  或拿去做未经许可的商业用途，由此产生的法律责任自行承担
- **深海舰旁边的「活动限定 · 活动已结束」是什么** → 它只在活动海域出现过，
  而那些海域已经撤了，现在遇不到。后续活动有可能再启用同一个形态。
  还开着的活动会标成「活动限定 · 现在还遇得到」
- **启动被 Windows 拦下** → 见第二节「首次运行会有安全提示」
- **代理装了自己的证书，kuma 仍然连不上** → 见下一节最后一条
- **某个面板显示「待补」或「◌」** → 那一格缺少判断所需的资料，见下一节第三条

## 六、已知限制

- 未做代码签名，首次运行有安全提示（第二节）
- 仅提供 Windows 64 位便携版，没有自动更新
- **部分社区资料因为上游没有授权再分发，没有随包**。受影响的功能会显示占位：
  明石改修的逐星加成、开发与建造配方、改造素材与回程成本、
  道具兑换目录、远征与任务前置的日文一手对照、路线页的日文分歧说明，
  以及新形态的舰娘档案、深海舰台词的补录层。
  （舰娘的中文台词、季节限定台词与字幕都**已经随包**，不在此列）
- **常规海域**的确认掉落、敌编成与限时掉落的起讫**已经随包**（37 张常规图，
  节点、敌编成 1312 套、掉落目录）。
  没有随包的只剩**活动海域**那几张图的详情（各难度层的节点、掉落、敌编成、解谜与
  基地航空所需半径），缺着时会显示「本地目录待更新」；活动图的敌情与掉落以你自己的实测记录为准
- 常规海域的**实测编成频率**（KCNav）不随包也不会自动获取：上游明确拒绝未授权的自动访问。
  需要的人可以从其官网手动导出后，放进 `%APPDATA%\kuma\lodes`
- **代理如果用了自己签发的证书**（例如岛风GO 的证书模式），而这张证书没有被系统信任，
  kuma 会拒绝这条连接。目前只能手工编辑 `%APPDATA%\kuma\config.json`：
  把 `kanso.network.customCertificateAuthority` 指向证书文件，
  或把证书的 sha256 加进 `kanso.trustedCerts`。界面上暂时没有入口
- 历史记录默认**不会自动清理**。「设置」→「记录保留与清理」里可以按月清，
  也可以填一个保留天数让它每天自动清；不填就是一直留着。
  遭遇志、任务进度、舰娘的人生记录、道具履历与活动履历不受任何清理影响
- 「设置」→「社区上报」里那两个开关尚未接入，是挂牌禁用状态
- 活动期间的资料需要随版本更新后才有，新活动开放当天可能暂缺
- 战斗预测与胜负判断是估算，以游戏结算为准（界面上已标 `≈` / `?`）

## 七、许可与鸣谢

kuma 显示的资料来自舰娘百科、KC3Kai、poi 等社区项目，逐项出处与许可见随附的
`NOTICE.md`，以及「设置」→「资料来源与许可」那一页

其中一部分按「非商业性使用」授权，所以 kuma 永久免费，也不得用于任何商业用途

kuma 与《艦隊これくしょん -艦これ-》的运营方无关，也未获其授权或认可
