# 域 F：任务前置链三项（2026-09-06，F48、B211、B216 全部已裁关闭）

## 只读勘探与数据结构

quests-scn 是游戏任务号→条目的对象，字段 code 为站内任务码、pre 为任务码数组；wikiwiki-quests 以任务码为键，另带 uncertain/aligned/condRaw。实际判定走 mergeQuestPre，不可把冲突标志或来源说明差异都算成前置集合变化。

原勘探报告的三项方向是“开发机→玩家”。初次用生产 mergeQuestPre 重算，三项均可复现；前两单分别从 `4aa6b5b`、`873253c` 续单，将 B211 裁为 `[F132]`、F48 裁为 `[F4,F44]`。本单从 `ff94b37` 续单，按三家正面主张（维护者核 2026-09-06），将 B216 裁为 `[B207]`（待实测）。三项仲裁至此全部关闭；B216 的实测状态及推翻条件另行保留。

## 三项主张与游戏内证据

| 游戏任务号 / 任务码 | scn主张（玩家） | wikiwiki主张 / 开发机合并结果 | 对照资料游戏证据 | 决定 |
| --- | --- | --- | --- | --- |
| 652 / F48 | C2、F44；现行玩家侧仲裁为 F4、F44 | F4、F44；现行开发机侧同样采用仲裁 | wikiwiki、英文 wiki、kcwiki 2017 更新页（维护者核 2026-09-06） | 已裁为 [F4,F44]；C2 为 kcwiki 独有主张，未采；Cd1 经 F44 传递不单列 |
| 1036 / B211 | 2507C1、F132 | F132；wikiwiki自标检证中；现行两侧仲裁均为 F132 | wikiwiki、totoneko 2025-07-26 攻略（维护者核 2026-09-06） | 已裁为 [F132]；2507C1 随活动过期不再作前置 |
| 1047 / B216 | 空数组，前置栏未登记；现行玩家侧仲裁为 B207 | B207；现行开发机侧同样采用仲裁 | wikiwiki、kobayangame、zekamashi 任務情報与评论区（维护者核 2026-09-06） | 已裁为 [B207]；仍保留可证伪性 |

F48的wikiwiki原始条件对F4还带“要検証”文字；B211的记录 uncertain=true。已有 mergeQuestPre 的悬空修补分支会保留可解析主张，和空前置补缺分支对 uncertain 的处理并不相同；本单未改该机制。

## B216 证据与裁决（本单，2026-09-06）

**B216/1047「【涼波改二】ラバウルより抜錨せよ！」前置裁为 `[B207]`，已有玩家实测，待独立复核。** B207/1023 为「三十二駆「藤波改二」、鳥海を護衛せよ！」（三十二驱藤波改二、护卫鸟海舰！）。依据为维护者 2026-09-06 深夜逐一核实的记录，本单只登记 URL 出处和裁决摘要，不出网、不抓取或整段转录页面正文。

| 来源 | B216 前置记录 | 裁量 |
| --- | --- | --- |
| [wikiwiki 任務/出撃任務](https://wikiwiki.jp/kancolle/任務/出撃任務) | 对照资料层明确列 B207 | 正面主张 |
| [kobayangame 2026-06-01 攻略](https://kobayangame.xyz/kancolle/20260529_ninmu6) | 前提任務明确列 B207 对应任务 | 正面主张 |
| [zekamashi ぜかまし B216 攻略](https://zekamashi.net/kancolle-kouryaku/suzunami-batubyou/) | B216 任務情報明确列 B207 对应任务 | 正面主张；评论区玩家实测进一步坐实 B207 为前置 |
| [kcwiki 现行任务页](https://zh.kcwiki.cn/wiki/任务#B216) | 随包 quests-scn 为 `pre=[]`；前置栏未登记 | 不视为「无前置」的正面主张 |
| [tsunkit 任务页](https://tsunkit.net/quests) | 前置栏未登记 | 不视为「无前置」的正面主张 |

三家明确主张一致，另两家空栏不构成反对票。该批 2025–26 新任务在 kcwiki 连任务名也有空栏，不能将资料缺漏解释成游戏中没有前置。**推翻条件：满足涼波改二的编成要求时，B207 未完成而 1047 出现。** 原报告“等 B207 完成后观察 1047 是否出现”不能代替此证伪条件，现予订正。

2026-09-06 出处补单（起点 `a54715e`）：[ぜかまし评论区第 1 页](https://zekamashi.net/kancolle-kouryaku/suzunami-batubyou/comment-page-1/)已有玩家实测，以下仅记维护者提供、统筹方已核的日期与原句要点：

- 5/30 16:06：「デイリー全部終わっても任務出ない」，藤波改二任务尚未完成，准备验证；同日 19:26 回报「藤波改二の任務もトリガーでした」。这组前后实测坐实 B207 为完成后才出现的前置。
- 5/30 16:49：报告「補給艦デイリー」达成时确认出现，推测「補給艦3隻撃破の方がトリガー」。
- 6/23 19:55：报告「デイリー10回〇 補給艦3隻〇 藤波改二〇 → 出た」，为三项均完成后出现的补充观察。

出现触发：多名玩家报告当日补给舰日常（3 隻）达成后才出现，属出现触发而非前置任务，未建模。日常补给舰任务疑为额外出现触发，仅在本报告记录，不进入 `pre`。

## B216 收录、消费与护栏（本单）

新增 B216 仲裁 1 项，表共 11 项；沿用 B211/F48 的 `pre`、`basis`、`evidence`、`date`，日期为 `2026-09-06`，原 `basis` 与 `evidence` 均标「待实测」；出处补单仅将 `evidence` 更新为「已有玩家实测，待独立复核」，`basis` 原文保留。实际生产装配的玩家与开发机两侧均为 `pre=[B207]`、`source=arbitrated`、`dangling=[]`，`scnPre=[]` 原样保留。玩家 `wwPre=null`、`conflict=false`；开发机 `wwPre=[B207]`、`conflict=true`。后者来自既有仲裁分支对两 wiki 集合不等的标记，不表示将 kcwiki 空栏裁成「无前置」主张。

新增行为用例验证玩家无 wikiwiki 与开发机有 B207 时的合并结果、来源及原始字段；表健康护栏更新为 11 项。`mergeQuestPre`、上游包及审计逻辑均未改。

## B216 玩家视角审计（本单）

前后生产审计均 EXIT=0。任务前置链缺／差／多由 `848/321/0` 变为 `847/321/0`，玩家／开发机有值格由 `3770/4618` 变为 `3772/4619`，白名单差异 `1169→1168`。完整指纹由 `608b659d9dd9c706bb683ecdca568089972073c3e05c27a372955267d89b2b3d` 更新为 `1abae3a2c7b3e9b4cea827a4bfb3920d3b7729035684f96f637219e9d6019421`。

逐格对拍仅 B216 三键变化：`pre/0`（玩家缺／开发机 B207）与 `source`（玩家 kcwiki／开发机 wikiwiki）两格差异消失；`conflict` 新增玩家 false／开发机 true 一格，净减 1 格。`wwPre/0` 的来源差异保留；玩家新增前置与依据两格、开发机新增依据一格，解释了有值格增量。其余任务差异、其余域审计输出、挂牌文案及前后主数据／维护者包哈希均一致。仅更新白名单「任务前置链」组的数量、指纹和原因。

日志目录：`<临时目录>/kuma-b216-20260906/`，包含 `audit-before.json`、`audit-after.json`、对应日志、`audit-delta.json`、`runtime.json` 与逐格复核脚本 `review.mjs`。

更新后的白名单复核 `parityErrors=[]`（`review.log`）。完整 URL 统一登记在本报告，运行时 `evidence` 沿用 B211/F48 的站点、日期、任务名体例并指向本报告：首轮把完整 URL 写进运行时证据字符串，触发了既有第三方地址与 tsunkit 地址两条护栏，3916 通过、2 失败、skipped 0、EXIT=1、`grep -c ERR_ASSERTION=4`；日志保留为 `test-first-failure.log`。修正只涉及出处存放形式，不改出口护栏、网络白名单或仲裁行为。

## B216 验收（本单）

按收尾定式重新完整执行：`npm test > <临时文件> 2>&1` **EXIT=0，3918 通过、失败 0、skipped 0、grep -c ERR_ASSERTION=0**；随后 `npx tsc --noEmit` EXIT=0，再 `npm run build` EXIT=0（`[kuma] build ok`）。相对起点的 3917 项新增 1 项 B216 行为用例。日志为上述目录的 `test.log`、`tsc.log`、`build.log`。未出网、未开窗、未推送；上游包、游戏报文和快照未写回。

出处补单验收（起点 `a54715e`，基线 3918）：按相同顺序执行，`npm test > <临时文件> 2>&1` EXIT=0，3918 通过、失败 0、skipped 0、`grep -c ERR_ASSERTION=0`，既有第三方地址与 tsunkit 地址护栏通过；随后 `npx tsc --noEmit` EXIT=0，`npm run build` EXIT=0（`[kuma] build ok`）。日志目录为 `<临时目录>/kuma-b216-evidence-20260906/`。首次 npm 启动受终端目录配置影响，在资料目录报 ENOENT，未执行测试，另存 `test-launch-failure.log`；显式重新定位仓库后完成上述验证。此次仅补出处文字与报告，无新增用例、判定或数据变更。

## B216 任务书核对（本单）

原任务书的起点 HEAD 与 3917 项基线吻合，裁决口径无误。原单未获ぜかまし文章完整 URL；本次出处补单已由维护者提供并由统筹方核实，完整 HTTPS 文章及评论页链接补入本报告，运行时 `evidence` 按既有 URL 护栏保留无协议的完整文章路径与评论页路径。只改 B216 的 `evidence` 文字与本报告，不改判定、`pre`、`basis`、`date`、数据或护栏。仲裁关闭与待独立复核并不矛盾：已有玩家实测，仍保留对照资料将来推翻的条件。

## 2026-09-06 B211/F48 续单证据与裁决（前单历史记录）

- **B211/1036「輸送船団護衛部隊、出撃せよ！」**：随包 quests-scn（kcwiki 2026.09.02）仍为 `[F132,2507C1]`；[wikiwiki 任務](https://wikiwiki.jp/kancolle/任務) 只列 F132，标検証中。维护者核实 [totoneko.net](https://totoneko.net/) 2025-07-26 的 B211 攻略页“前提任務”仅列「【対潜戦力強化】海防艦対潜兵装の強化」（F132），并核实限时 2507C1 随 2025-07 活动过期不再作前置。按两票对一票及该过期事实，现行前置裁为 `[F132]`，核实日期 `2026-09-06`。本单未提供 totoneko 文章完整 URL，故只记站点、日期与任务名，不猜造文章地址。此裁决仅适用于 B211，不推导其他限时前置自动失效。
- **F48/652「特注家具」の調達（“特注家具”的调拨）**：本单新增英文 wiki 页面记录口径，并按维护者裁定将四份记录逐项对照。前单“2017 页仅作历史证据、F48 不裁”的决定已由本单取代；2017 页仍标明实装当天记录，本次用于佐证 F4，未把整套历史前置照搬为现行链。

| 来源 | F48 前置主张 | 本单证据说明 |
| --- | --- | --- |
| [kcwiki 现行「任务」F48](https://zh.kcwiki.cn/wiki/任务#F48) | F44、C2 | 随包 quests-scn 2026.09.02 |
| [kcwiki「游戏更新/2017年1月10日」](https://zh.kcwiki.cn/wiki/游戏更新/2017年1月10日) | C2、F4、B89 | 实装当天记录；维护者已核实 |
| [wikiwiki 任務/工廠任務](https://wikiwiki.jp/kancolle/任務/工廠任務) | F4、F44 | 对照资料层；F4 标「要検証」 |
| [英文 wiki](https://en.kancollewiki.net/) | Cd1、F4、F44 | 维护者页面记录核实；未提供页面记录页面完整 URL，只登记站点 |

**裁决（2026-09-06）：F48 前置为 `[F4,F44]`。** F44 获 kcwiki 现行页、wikiwiki、英文 wiki 三份记录支持；F4 获 wikiwiki、英文 wiki、kcwiki 2017 页三份记录支持。C2 为 **kcwiki 独有主张，未采**（现行与 2017 两页均有，但同属 kcwiki）。Cd1 仅英文 wiki 列出，包内 F44/648 的 `pre=[Cd1,F42]` 已使其经 F44 传递隐含，不单列；B89 仅 2017 页，未采。四份来源记录涉及三个站点，不称为四个独立站点一致，也不称为三份记录对整套 `[F4,F44]` 一致。

以上网页内容由维护者核过，本单仅登记出处及裁决摘要，不出网、不抓取或转录页面正文。前单 [任务包刷新报告](quests-scn-refresh-20260906.md) 中“页面记录页面尚未定位”是当时结论，以本次历史页面定位补记为准；上游包和解析器均未改。

## 证据范围

2026-09-06：前置关系依据下列公开站点的任务资料裁定；个人取证材料另存维护者侧，不作为公开出处。

## F48 收录、消费与护栏（前单历史记录）

新增 F48 仲裁 1 项，表共 10 项；沿用 B211 的 `pre`、`basis`、`evidence`、`date` 体例，日期为 `2026-09-06`。玩家与开发机运行时均为 `pre=[F4,F44]`、`source=arbitrated`、`dangling=[]`；`scnPre=[C2,F44]` 原样保留，开发机仍带出 `wwPre=[F4,F44]` 和 `conflict=true`，玩家为 `wwPre=null`、`conflict=false`。对照资料 F4 的「要検証」保存在 `condRaw`，F48 条目未设 `uncertain=true`，不混同 B211 的字段状态。

新增行为用例覆盖玩家、开发机及 C2 将来可解析时的结果，确保不依赖悬空修补才得到裁决；同时更正旧护栏注释中“C2 是改号前旧码”的无依据推断。`mergeQuestPre`、上游包与审计逻辑均未改，B211 和原三条保留限时码的裁决保持不变。

## F48 玩家视角审计（前单历史记录）

前后生产审计均 EXIT=0。任务前置链缺／差／多由 `848/322/0` 变为 `848/321/0`；玩家／开发机有值格仍为 `3770/4618`，白名单差异 `1170→1169`。完整指纹由 `74497419ed37f23196e27fe1387208f37bc07967b0e2e33e10fd1cf685fdbd9d` 更新为 `608b659d9dd9c706bb683ecdca568089972073c3e05c27a372955267d89b2b3d`。

逐格对拍仅 F48 三键变化：`pre/0`（玩家 C2／开发机 F4）、`source`（玩家 kcwiki／开发机 merged）两格差异消失，既有仲裁分支保留原始双 wiki 分歧使 `conflict` 新增玩家 false／开发机 true 一格，净减 1 格。只按此结果更新任务组白名单的数量、指纹和原因，其余任务格键、两侧值及另 9 组白名单不变；其余域输出、挂牌文案与前后主数据／维护者包哈希均一致。

日志目录：`<临时目录>/kuma-f48-20260906/`，包含 `audit-before.json`、`audit-after.json`、对应日志、`audit-delta.json`、`runtime.json` 与 `quest-pre-test.log`。更新后的白名单检查 `parityErrors=[]`（`parity.log`），实际两侧装配的 F48 前置及 F44 的 `[Cd1,F42]` 也已断言核对。

## F48 验收（前单历史记录）

按收尾定式执行：`npm test > <临时文件> 2>&1` **EXIT=0，3917 通过、失败 0、skipped 0、grep -c ERR_ASSERTION=0**；随后 `npx tsc --noEmit` EXIT=0，再 `npm run build` EXIT=0（`[kuma] build ok`）。相对 HEAD 的 3916 项增加 1 项 F48 行为用例，任务前置定向测试 9 项通过。日志为上述目录的 `test.log`、`tsc.log`、`build.log`。

首次测试启动时，尽管命令指定了仓库工作目录，npm 实际在主目录寻找 package.json，以 ENOENT／EXIT=-4058 退出，尚未运行测试；日志保留为 `test-first-launch-failure.log`。随后关闭登录 shell 配置加载并显式 `Set-Location` 到仓库，完整执行上述验收顺序通过。未出网、未开窗、未推送；上游包、游戏报文和快照未写回。

## F48 任务书需澄清（前单历史记录）

1. “四源”指四份记录、三个站点；kcwiki 两页属于同站不同时期。F4 与 F44 各获三份记录支持，但支持它们的三份记录不同，不能表述为三个站点对完整前置集合一致。
2. 英文 wiki 只给了域名及维护者页面记录核实结论，没有完整页面 URL；本单按授权记站点出处，不出网补抓、不猜造深链接。

## B211 收录、消费与护栏（前单历史记录）

本次新增 B211 仲裁 1 项，表共 9 项；条目保留供详情展示的 basis，并新增可选 evidence、date 元数据记录本次证据。mergeQuestPre、任务面板、完整任务树和上游包未改。运行时玩家及开发机均为 `pre=[F132]`、`source=arbitrated`、`dangling=[]`；原始 scnPre 仍保留 2507C1，开发机仍显示双 wiki 分歧及 uncertain 信息。新增行为护栏覆盖无 wikiwiki 的玩家侧与 uncertain=true 的开发机侧，旧三条保留限时码的裁决护栏继续生效。

## B211 玩家视角审计（前单历史记录）

任务前置链缺／差／多从 `848/323/1` 变为 `848/322/0`，玩家／开发机 `3770/4618` 格，白名单差异 `1172→1170` 格，指纹从 `c695754b93c21fa59ab9c28eea039ed449981fe425fdcea660c3fb2825ec8cdb` 更新为 `74497419ed37f23196e27fe1387208f37bc07967b0e2e33e10fd1cf685fdbd9d`。

仅 B211 四个差异键变化：`pre/0`、`pre/1`、`source` 三格消失；既有仲裁分支如实保留双 wiki 分歧，新增 `conflict`（玩家 false／开发机 true）一格，净减 2 格。其余任务差异、其余域输出及另 9 组白名单指纹不变，前后主数据和维护者包哈希一致。白名单仅按此审阅结果更新任务组的数量、指纹与原因，不改审计逻辑；`parityErrors=[]`。

日志目录：`<临时目录>/kuma-b211-20260906/`，包含审计前后 JSON、`audit-delta.log` 和运行时两侧输出。首次仲裁后独立审计以 Windows `0xC0000374` 退出，未产生输出，保留 `audit-after-first-failure.log`；原命令重跑 EXIT=0，未修改生产或审计代码绕过。

## B211 验收（前单历史记录）

按收尾定式顺序执行：`npm test > <临时文件> 2>&1` EXIT=0，**3916 通过、失败 0、skipped 0、grep -c ERR_ASSERTION=0**；随后 `npx tsc --noEmit` EXIT=0，`npm run build` EXIT=0（`[kuma] build ok`）。相对施工单 3915 基线新增 1 项 B211 行为用例；任务前置定向测试 8 项通过。日志为上述临时目录的 `test.log`、`tsc.log`、`build.log`、`quest-pre-test.log`。未写回游戏报文、快照或上游包，未出网、未开窗、未推送。

## B211 任务书需订正（前单历史记录）

1. 既有仲裁条目只有 pre、basis，没有 evidence、date；本次以可选字段补充证据和日期，兼容旧 8 条及原 mergeQuestPre 接口。
2. totoneko 只给了站点、日期与任务身份，没有完整文章 URL；本单禁出网，报告据维护者核实记来源，未补造深链接。
3. B211 的最终前置在开发机原已因悬空修补为 `[F132]`，本次让玩家侧也采用第一方裁决；审计差异仍包含来源元数据，不能要求任务组差异全部归零。F48 旧报告的“页面记录未定位”已补记为历史页面。

## 初次勘探验收（历史记录）

npm test最终：3844通过、EXIT=0、grep -c ERR_ASSERTION=0、skipped=0；npx tsc --noEmit 与 npm run build 均 EXIT=0。日志：TEMP/kuma-medium-20260906/F-test.log、F-tsc.log、F-build.log。未开窗、未推送。

首次整套测试曾在既有玩家视角审计的活动特效scopeApplies分支报TypeError（读取undefined.has，3843通过/1失败，ERR_ASSERTION=0）。单独重跑player-view-parity的6项全部通过，随后不改代码完整复跑通过。首次失败保存为F-test-first-failure.log，单独复跑为F-parity-recheck.log。原因尚未定位，没有通过跳过测试或任务外防御修改掩盖。

## 初次勘探任务书需订正（历史记录）

1. 标题说五域，正文实际是A–F六域，本批按六域独立提交。
2. 原报告“三项”指最终前置集合；当前玩家视角审计还统计来源、分歧标志及说明叶子，所以任务域的总差异格数远大于3，不能据此扩大仲裁范围。
3. 前置资料没有游戏直接下发的边字段，不能把任务缺席解释成前置结论。
4. E域的“页面有条目”不等于统一模块有记录，详见该域报告；A域还存在数值冲突以外的旧解析身份问题。
