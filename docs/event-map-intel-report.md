# 活动海域维护者订正 · 2026-09-06

## E5 甲奖励维护者订正

起点 `3ebfed7`。原先因舰娘百科原文缺选择分隔符而剔除的 E5 甲 Bofors／格纳库增设组，按统筹方当日核对第三票后的裁定补回二选一，位置在 V-156F 选择组之后，沿用舰娘百科原顺序。

订正表位于 `scripts/lib/event-map-intel.mjs` 的 `MAINTAINER_REWARD_CORRECTIONS`，内容如下：

```json
{
  "event": "反撃！第三十一戦隊の戦い",
  "map": "62-5",
  "difficulty": "甲",
  "domain": "rewards",
  "after": "【V-156F(SB2U输出型)★+4 / 格纳库增设×4】",
  "reward": "【Bofors 12cm单装两用炮★+4 / 格纳库增设×3】",
  "basis": "maintainer",
  "evidence": "https://zekamashi.net/202607-event/hangeki31sentai-nannido/；乙/丙同型选择组与随包一致；舰娘百科原文的 Bofors 与格纳库增设组缺选择分隔符；统筹方 2026-09-06 核对第三票后裁定为二选一。",
  "date": "2026-09-06"
}
```

URL 仅作出处，依据来自施工单，本次未出网、未抓取或转录该站文字。解析器仍保留原始 `ambiguous-choice` 问题报告，生成器在对照和 E1 步骤订正后应用奖励订正；不会把裁定写回上游缓存或参考包。

来源放在甲奖励行的 `rewardSources[完整选择组文本]`，只标记新增组，不把整行或乙／丙同型组标成维护者订正。类型与校验支持该字段，拒绝非法来源形状、basis、空 evidence、错误日期格式和不属于完整奖励组的来源键。

### 缓存重生成与四难度逐字核对

使用仓库 `test/fixtures/kcwiki-event-map-E{1..5}-20260906.html.gz` 解压到绝对临时目录，生成器通过 `--from-file` 离线读取，`--compare-from` 指向只读 `assets/lodes/map-intel.json`，写回随包 `assets/lodes/event-map-intel.json`。

| 难度 | 奖励行原文 |
| --- | --- |
| 甲 | `【G-36A(F4F输出型)★+5 / 海外舰最新技术×5】、【V-156F(SB2U输出型)★+4 / 格纳库增设×4】、【Bofors 12cm单装两用炮★+4 / 格纳库增设×3】、【F4U-7★+1】、【改修资材×10】、【勋章×3】、【夜间熟练搭乘员×1】、【甲种勋章】` |
| 乙 | `【G-36A(F4F输出型)★+3 / 海外舰最新技术×4】、【Bofors 12cm单装两用炮★+2 / 格纳库增设×3】、【改修资材×8】、【勋章×2】、【夜间熟练搭乘员×1】` |
| 丙 | `【G-36A(F4F输出型) / 海外舰最新技术×2】、【Bofors 12cm单装两用炮★+1 / 格纳库增设×1】、【改修资材×5】、【勋章×1】` |
| 丁 | `无` |

与起点包逐字段比对，除 `meta.fetchedAt` 外，仅 E5 甲奖励行及该组来源变化；乙／丙／丁、共通奖励、E1 已有订正及其它域和图均不变。

### 护栏、口径与验收

新增 2 条行为用例，覆盖订正活动／图／难度／域限定、插入位置、缺锚点不补、输入不变、幂等、四难度逐字奖励和乙／丙同型格式。既有用例增加来源校验反例、生产渲染和离线 CLI 产物／订正报告断言。

生产口径审计 EXIT=0：活动奖励仍为 `0/0/0`，玩家／开发机均 20 格；白名单无活动奖励条目，本次无须修改。

按序验收：`npm test > <绝对临时文件> 2>&1` EXIT=0，3872 通过、fail 0、`grep -c ERR_ASSERTION`=0、skipped 0；随后 `npx tsc --noEmit` EXIT=0，`npm run build` EXIT=0（`[kuma] build ok`）。相较起点 3870，新增 2 条用例。生成报告、测试、类型检查、构建和口径审计日志位于 `<临时目录>/kuma-e5-reward-20260906`。

任务书有一处描述与起点不符：本报告在 `3ebfed7` 只有 E1 丙订正记录，没有“E5 甲歧义组剔除”段落；本次新增 E5 甲维护者订正记录，保留下面的 E1 历史记录。

## E1 丙 P1 维护者订正（既有记录）

起点 `84b07ca`。本次将 E1 丙 P1 C2/C3 从“两站冲突未收”改为“维护者订正”：两步均为 A 胜 ×1。其它冲突与未收项沿用既有处理。

订正表位于 `scripts/lib/event-map-intel.mjs` 的 `MAINTAINER_GIMMICK_CORRECTIONS`，逐条限定活动 `反撃！第三十一戦隊の戦い`、图 `62-1`、难度 `丙`、标题 `E1P1解谜` 和点位 `C2`／`C3`。生成器在两站对照后应用，保留原始解析与冲突报告，不把裁定写回上游。

| 点位 | 随包步骤原文 | basis | date |
| --- | --- | --- | --- |
| C2 | `C2点：A胜利 ×1` | `maintainer` | `2026-09-06` |
| C3 | `C3点：A胜利 ×1` | `maintainer` | `2026-09-06` |

两条 `evidence` 相同，内容为：

```text
https://zekamashi.net/202607-event/dai31sentai-syutugeki-1/；wikiwiki 同值：E1 丙 P1 C2/C3 A胜×1；维护者核 2026-09-06。
```

公开来源经维护者核 2026-09-06，URL 仅作出处。舰娘百科缓存的丙 C2/C3 为 A 胜及以上 ×2，与 wikiwiki 缓存的 A 胜 ×1 冲突；zekamashi 与 wikiwiki 同值，支持本次裁定（维护者核 2026-09-06）。

步骤继续使用字符串列表，来源放在同组 `stepSources[完整步骤文本]`，只标这两步，不将同组 H 或其它难度冒充维护者订正。类型与包校验同步支持该字段；非法 basis、空 evidence、脱离步骤列表的来源键会被拒绝。

### 缓存重生成与逐字核对

输入为仓库 `test/fixtures/kcwiki-event-map-E{1..5}-20260906.html.gz` 解压后的五页 HTML；`--from-file` 指向绝对临时目录，`--compare-from` 指向只读 `assets/lodes/map-intel.json`，通过生成器写回 `assets/lodes/event-map-intel.json`。无网络请求；未手改上游包或维护者包。

E1 P1 重生成后的步骤逐字如下；“无此步骤”表示该难度 P1 列表中无 F：

| 难度 | C2 | C3 | H | F |
| --- | --- | --- | --- | --- |
| 甲 | `C2点：S胜利 ×2` | `C3点：S胜利 ×2` | `H点：到达 ×1` | `F点：空中优势及以上 ×2` |
| 乙 | `C2点：S胜利 ×1` | `C3点：S胜利 ×1` | `H点：到达 ×1` | 无此步骤 |
| 丙 | `C2点：A胜利 ×1` | `C3点：A胜利 ×1` | `H点：到达 ×1` | 无此步骤 |
| 丁 | `C2点：A胜利及以上 ×1` | `C3点：A胜利及以上 ×1` | `H点：到达 ×1` | 无此步骤 |

按胜利等级／空优／到达与次数核对，均与施工单给定第三票一致，无新增冲突。甲 F 的“空中优势及以上”、丁的“A胜利及以上”保留现有缓存文本。包 diff 除生成时间外，仅增加丙 C2/C3 和各自来源；甲／乙／丁、P2／P3、其它图无变化。

### 玩家视角与护栏

生产审计活动机关仍为 `0/4/0`，玩家／开发机均 20 格。全部差异属于 E4 四难度（`62-4/丁`、`62-4/丙`、`62-4/乙`、`62-4/甲`），指纹仍为 `751f36686931b75eb3df09cd35508c39eb39a6aed5f7337aa63a702bff61e323`。白名单保留 4 格，仅更新原因，明确 E4 范围；未达到退役或改数条件。

新增行为用例覆盖：精确订正作用域、输入不变、重复应用幂等、两站对照剔除后恢复、其它难度／阶段／图／活动不变、随包四难度逐字内容和两步来源。离线 CLI 用例验证实际生成结果与订正报告。

最终按序验收：`npm test > <绝对临时文件> 2>&1` EXIT=0，3870 通过、fail 0、`grep -c ERR_ASSERTION`=0、skipped 0；随后 `npx tsc --noEmit` EXIT=0，`npm run build` EXIT=0（`[kuma] build ok`）。相较派单基线 3868，新增 2 条用例。日志保存在 `<临时目录>/kuma-e1-correction-20260906`。初次类型检查的回调数组收窄错误已修复，以上结果来自修复后重新完整执行的收尾序列。

任务书需澄清两点：原 `docs/event-map-intel-report.md` 在起点不存在，本次新增；“活动机关 0/4”并非 E1 两步骤造成，实际是 E4 四难度的整段渲染差异，因此本单不应将它退役或减数。
